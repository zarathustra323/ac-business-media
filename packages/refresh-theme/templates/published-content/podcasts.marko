import defaultValue from "@base-cms/marko-core/utils/default-value";

$ const title = defaultValue(data.title, "Podcasts");

<refresh-theme-published-content-layout title=title>
  <@query
    name="all-published-content"
    params={
      contentTypes: ["Podcast"],
      limit: 12,
    }
  />
</refresh-theme-published-content-layout>
